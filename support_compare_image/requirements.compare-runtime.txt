# Runtime dependencies for the optional database-backed comparison worker.
# The worker loads the Hugging Face model lazily; keeping these dependencies in
# a separate file makes the phase-1 model choice explicit.
numpy>=1.26
pillow>=10.0
imagehash>=4.3
scikit-image>=0.24
torch>=2.4,<3
torchvision>=0.19,<1
transformers>=4.45,<6
