# Only needed to run the schema migrations (alembic upgrade head) from this folder.
# Use the production database through a private network or SSH tunnel; never commit the real file.
DATABASE_URL=postgresql+psycopg://USER:PASSWORD@127.0.0.1:15432/DB_NAME
